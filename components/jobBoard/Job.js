import SearchBar from '../SearchBar';
import JobList from './JobList';

const Job = () => {
  return (
    <div className="">
      <SearchBar />
      <JobList />
    </div>
  );
};

export default Job;
